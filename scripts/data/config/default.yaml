defaults:
  - _self_
  - override hydra/launcher: basic

hydra:
  output_subdir: null
  mode: RUN
  run:
    dir: .

cache_dir: null
dataset_name: debug_tworoom

world:
  num_envs: 10
  max_episode_steps: 100
  image_shape: [224, 224]

num_workers: 4
seed: 3072
num_traj: 10000

options: null